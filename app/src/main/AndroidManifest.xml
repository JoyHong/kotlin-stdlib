<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--    &lt;!&ndash;用于访问GPS定位&ndash;&gt;-->
    <!--    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />-->
    <!--    &lt;!&ndash;用于进行网络定位&ndash;&gt;-->
    <!--    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />-->
    <!--    &lt;!&ndash; Required only when requesting background location access on Android 10 (API level 29) and higher. &ndash;&gt;-->
    <!--    &lt;!&ndash; 在较低版本的 Android 系统中, 当应用获得前台位置信息访问权限时, 也会自动获得后台位置信息访问权限 &ndash;&gt;-->
    <!--    &lt;!&ndash; 在搭载 Android 10（API 级别 29）的设备上, 您的应用中的某项功能请求在后台访问位置信息时, 系统权限对话框包含一个名为始终允许的选项.如果用户选择此选项, 您的应用中的相应功能就会获得在后台访问位置信息的权限. &ndash;&gt;-->
    <!--    &lt;!&ndash; 但是，在 Android 11（API 级别 30）及更高版本中, 系统对话框不含始终允许选项. 相反, 用户必须在设置页面上启用后台位置信息 &ndash;&gt;-->
    <!--    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />-->

    <!-- 告知系统应用打算设置配套设备 -->
    <uses-feature android:name="android.software.companion_device_setup" />
    <!-- 蓝牙的配对 -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinStdlib">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>